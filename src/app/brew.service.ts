import { Injectable } from '@angular/core';
import { BrewProfile, ExtractionResult } from './brew_profile';
import { Observable, Subject, interval, map } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class BrewService {

 

  constructor() { 
  }

  ngOnInit(): void {
    
  }

  
}